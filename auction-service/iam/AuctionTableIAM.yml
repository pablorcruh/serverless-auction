AuctionTableIAM:
  Effect: Allow
  Action:
    - 'dynamodb:PutItem'
    - 'dynamodb:scan'
    - 'dynamodb:GetItem'
    - 'dynamodb:UpdateItem'
    - 'dynamodb:Query'
  Resource: 
    - ${self:custom.AuctionTable.arn}